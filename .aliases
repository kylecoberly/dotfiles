# Scripts
function mkcd () {
    mkdir $1
    cd $1
}

# Shortcuts
alias c="clear"
alias j="jobs"
alias update="sudo apt-get update && sudo apt-get upgrade"

# Overwrites
alias vi="vim"
alias mv="mv -i"
alias cp="cp -i"
