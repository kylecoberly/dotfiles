bindsym $mod+q kill
bindsym $mod+Return exec alacritty
## Print system info
bindsym $mod+i exec \
	notify-send --expire-time="$display_time" --urgency=low "$(sh -c ~/dotfiles/window-manager/ubuntu/i3/scripts/system-info.sh)"
bindsym $mod+Space exec rofi -show run
