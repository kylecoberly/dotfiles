# Starship prompt — Tokyo Night palette, agnoster-inspired Powerline style
# Requires a Nerd Font (Alacritty is configured for "NotoMono Nerd Font").
# Palette uses TN's distinctive purple / blue / cyan accent trio.

add_newline = false
command_timeout = 500

format = """\
$os\
$directory\
$git_branch\
$git_status\
$nodejs$python$golang$rust$java\
$cmd_duration\
$line_break\
$character"""

# ─── OS icon (leftmost segment) — TN purple ────────────────────────────
[os]
disabled = false
format = "[ $symbol ](bg:#bb9af7 fg:#1a1b26)"

[os.symbols]
Macos  = ""
Linux  = ""
Ubuntu = ""

# ─── Current directory — TN blue ───────────────────────────────────────
[directory]
format = "[](fg:#bb9af7 bg:#7aa2f7)[ $path ](bg:#7aa2f7 fg:#1a1b26)"
truncation_length = 3
truncation_symbol = "…/"
home_symbol = "~"
read_only = "  "

# ─── Git branch — TN cyan ──────────────────────────────────────────────
[git_branch]
symbol = ""
format = "[](fg:#7aa2f7 bg:#7dcfff)[ $symbol $branch ](bg:#7dcfff fg:#1a1b26)"

[git_status]
format = "[$all_status$ahead_behind](bg:#7dcfff fg:#1a1b26)[](fg:#7dcfff)"
modified   = "${count}● "
staged     = "${count}+ "
untracked  = "${count}? "
ahead      = "⇡${count} "
behind     = "⇣${count} "
diverged   = "⇕${count} "
conflicted = "!${count} "
deleted    = "${count}✖ "
renamed    = "${count}» "
stashed    = "${count}* "

# ─── Runtime versions (only when project files present) ────────────────
# Each runtime uses a TN-palette-accurate accent tied to its ecosystem
[nodejs]
format = "[  $symbol $version ](fg:#9ece6a)"
symbol = ""
detect_files = ["package.json", ".nvmrc", ".node-version"]

[python]
format = "[  $symbol $version ](fg:#e0af68)"
symbol = ""
detect_extensions = ["py"]
detect_files = ["pyproject.toml", "requirements.txt", ".python-version", "Pipfile"]

[golang]
format = "[  $symbol $version ](fg:#7dcfff)"
symbol = ""
detect_files = ["go.mod", "go.sum"]

[rust]
format = "[  $symbol $version ](fg:#f7768e)"
symbol = ""
detect_files = ["Cargo.toml"]

[java]
format = "[  $symbol $version ](fg:#ff9e64)"
symbol = ""
detect_files = ["pom.xml", "build.gradle", "build.gradle.kts", "deps.edn"]

# ─── Command duration ──────────────────────────────────────────────────
[cmd_duration]
format = "[ took $duration ](fg:#ff9e64)"
min_time = 2000

# ─── Prompt character (vi-mode aware) ──────────────────────────────────
[character]
success_symbol = "[❯](bold #9ece6a)"
error_symbol   = "[❯](bold #f7768e)"
vimcmd_symbol  = "[❮](bold #7aa2f7)"
